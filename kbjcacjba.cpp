#include <bits/stdc++.h>
using namespace std;
int main () {
	int t; cin >> t;
	for (int j=0; j<t; j++) {
		int n,x,a,b;
		cin >> n >> x >> a >> b;
		int dis=0;
		dis=abs(a-x);
		cout << dis << endl;
	}
}
